# User experience

The primary layout is a collapsible project tree, central editor, and PDF preview.
Problems, logs, Command Finder, outline, and notation tools are secondary panels.

Keyboard operation is first-class. Risky actions explain their effect before
consent. Dirty, saving, failed, and conflicted states are distinct. External
changes to a dirty buffer always require explicit resolution. Failed builds retain
the last successful PDF.
