# Coding-agent task template

Every implementation request must state: task ID, title, goal, context, expected
modules, dependencies, requirements, non-goals, acceptance criteria, required
tests, and manual verification.

Agents must verify dependencies, keep the change task-scoped, update tests and
documentation together, report acceptance evidence and residual risks, and stop
for an ADR amendment if an invariant or public interface must change.
