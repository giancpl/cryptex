{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the main CrypTex window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
