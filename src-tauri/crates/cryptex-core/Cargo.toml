[package]
name = "cryptex-core"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
thiserror = "2"
ts-rs = { version = "11", features = ["serde-compat"] }

[dev-dependencies]
serde_json = "1"
tempfile = "3"

